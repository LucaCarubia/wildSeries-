{% extends 'base.html.twig' %}

{%  block body %}
        <h2>Titre : {{ program.title }}</h2>
        <a href="http://127.0.0.1:8000/program/{{ program.slug }}"><h1>{{ program.title }}</h1></a> vient d'être publiée sur Wild
        Séries !
{% endblock %}